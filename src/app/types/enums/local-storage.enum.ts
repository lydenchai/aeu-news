export enum LocalStorageEnum {
  Token = '_t',
  RefreshToken = '_rt',
  ForceChangePassword = '_fcp',
  UserId = '_uid',
  Username = '_username',
  UserProfile = '_user_profile',
  NewsId = '_news',
}
